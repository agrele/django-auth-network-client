============
Installation
============

At the command line::

    $ easy_install django-auth-network-client

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-auth-network-client
    $ pip install django-auth-network-client
